services:
  front_proxy:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - backend
